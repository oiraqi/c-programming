#define MAX_SEQUENCE 90
unsigned long fib_numbers[MAX_SEQUENCE];
int is_prime(unsigned long); /* Function prototype or signature */
unsigned long fib(int);
unsigned long rfib(int);
int fact(int);
float max(float, float);
float min(float, float);
void init(void);
unsigned long rfib_optimized(int);
